import { ObjValuesPipe } from './obj-values.pipe';

describe('ObjValuesPipe', () => {
  it('create an instance', () => {
    const pipe = new ObjValuesPipe();
    expect(pipe).toBeTruthy();
  });
});
